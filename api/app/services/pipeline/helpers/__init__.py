"""Pipeline helper modules.

Reusable, deterministic aggregation primitives that operate on already-normalized
pipeline data (e.g., NORMALIZE_PBP output). Distinct from `stages/`, which contains
end-to-end pipeline stage implementations.
"""
