// TypeScript-ESLint still requires the TypeScript 6 compiler API. Keeping its
// dependency graph in this package lets the application compile with TS 7.
export { default } from "eslint-config-next";
